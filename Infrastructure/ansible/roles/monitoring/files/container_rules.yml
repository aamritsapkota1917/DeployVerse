groups:
  - name: container.rules
    rules:
      - alert: ContainerStopped
        expr: engine_daemon_container_states_containers{state="stopped"} > 0
        for: 1m
        labels:
          severity: warning
        annotations:
          summary: "A container has stopped"
          description: "Container '{{ $labels.name }}' on '{{ $labels.instance }}' has stopped!"