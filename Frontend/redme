trivy scanner 


git add .
 git commit -m "feat(CSS): add fix index css1  "
 git push -u origin feature/post-blog-data
 gh pr create --base Dev --head "$(git branch --show-current)" --title "pr from feaure to dev" --body "s… 
 gh pr merge $(gh pr list --limit 1 --json number --jq '.[0].number') --merge
 gh pr create --base main --head Dev --title "PR:  dev to main " --body "Some fixes"
 gh pr merge $(gh pr list --limit 1 --json number --jq '.[0].number') --merge
   
   yo  yoy o  jho uhuoip 